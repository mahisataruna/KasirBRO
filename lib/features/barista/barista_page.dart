import 'package:flutter/material.dart';

class BaristaPage extends StatelessWidget {
  const BaristaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Barista')));
  }
}
